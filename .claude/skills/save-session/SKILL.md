---
name: "save-session"
description: "Save the current session state to memory so it can be resumed later. Use when: finishing work, end of day, before closing VS Code, save session, checkpoint progress."
user-invocable: true
---

Read the Copilot prompt definition at `.github/prompts/save-session.prompt.md` for full instructions, then execute that workflow.

`.github/prompts/save-session.prompt.md` is the single source of truth for this capability and is shared with GitHub Copilot. Do not duplicate its content here.
