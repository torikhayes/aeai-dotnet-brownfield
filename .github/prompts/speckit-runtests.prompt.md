---
name: "Speckit Run Tests"
description: "Run the shell-scripted integration tests for the currently active feature spec. Executes tc-*.sh scripts via run-all.sh and reports results. Run after /speckit-scripttest."
agent: agent
---

Read the Claude skill definition at `.claude/skills/speckit-runtests/SKILL.md` for full instructions, then execute that workflow.

Arguments passed to this command (if any) should be forwarded as-is to the skill.
