---
name: "Speckit Script Test"
description: "Generate shell scripts that automate the test cases in a feature's test plan markdown files. Spawns one sub-agent per independent markdown file for parallelism."
agent: agent
---

Read the Claude skill definition at `.claude/skills/speckit-scripttest/SKILL.md` for full instructions, then execute that workflow.

Arguments passed to this command (if any) should be forwarded as-is to the skill.
